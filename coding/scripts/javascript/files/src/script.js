// acolando espaço na memória para um texto
const message = "Hello world!"

// atualizando texto do #header com `message`
document.querySelector("#header").innerHTML = message

// imprimindo `message` no console
console.log(message)